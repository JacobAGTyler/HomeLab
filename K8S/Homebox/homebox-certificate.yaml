apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homebox-tls
  namespace: homebox
spec:
  secretName: homebox-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: homebox.jacobagtyler.com
  dnsNames:
    - homebox.jacobagtyler.com
