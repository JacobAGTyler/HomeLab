apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: district-badges-test
  namespace: argocd
spec:
  project: lbd-apps
  source:
    repoURL: https://github.com/LBDistrictScouts/DistrictBadges.git
    targetRevision: dev
    path: K8S/overlays/test
  destination:
    server: https://kubernetes.default.svc
    namespace: district-badges-test
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
