apiVersion: apps/v1
kind: Deployment
metadata:
  name: truenas-hdd-nfs-provisioner
  namespace: truenas-nfs-provisioner
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: truenas-hdd-nfs-provisioner
  template:
    metadata:
      labels:
        app: truenas-hdd-nfs-provisioner
    spec:
      serviceAccountName: nfs-subdir-external-provisioner
      containers:
        - name: nfs-subdir-external-provisioner
          image: registry.k8s.io/sig-storage/nfs-subdir-external-provisioner:v4.0.2
          imagePullPolicy: IfNotPresent
          env:
            - name: PROVISIONER_NAME
              value: homelab.jacobagtyler.com/truenas-hdd-nfs
            - name: NFS_SERVER
              value: terra-nas.jacobagtyler.com
            - name: NFS_PATH
              value: /mnt/HDDs/K8S
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
              cpu: 200m
          volumeMounts:
            - name: nfs-client-root
              mountPath: /persistentvolumes
      volumes:
        - name: nfs-client-root
          nfs:
            server: terra-nas.jacobagtyler.com
            path: /mnt/HDDs/K8S
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: truenas-capacity-rwx
provisioner: homelab.jacobagtyler.com/truenas-hdd-nfs
allowVolumeExpansion: true
reclaimPolicy: Delete
volumeBindingMode: Immediate
parameters:
  pathPattern: "${.PVC.namespace}/${.PVC.name}"
  onDelete: retain
mountOptions:
  - nfsvers=4.1
  - hard
  - noatime
